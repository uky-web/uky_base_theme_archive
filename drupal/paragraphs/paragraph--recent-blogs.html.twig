{% set type_tid = paragraph.field_news_type.target_id %}
{% set type_name = paragraph.field_news_type.entity.name.value %}
{% if not (type_name ends with 's') %}
{% set type_name = type_name ~ 's' %}
{% endif %}
{% include '@organisms/news-collection.html.twig' with {
    collection_headline: paragraph.field_title.value,
    collection_headline_modifier: 'section-title',
    buttons: [
        {
            title: 'View all ' ~ type_name,
            url: '/news-archive?news-type=' ~ type_tid,
        }],
    stories: content.recent_blogs_entity_view_1,
} %}